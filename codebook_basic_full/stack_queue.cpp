// Stack & Queue
#include <bits/stdc++.h>
using namespace std;

int main() {
    stack<int> st;
    st.push(1);
    st.push(2);
    st.pop();

    queue<int> q;
    q.push(3);
    q.push(4);
    q.pop();

    return 0;
}